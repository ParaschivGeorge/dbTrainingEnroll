spring.datasource.url=jdbc:mysql://185.162.65.163:3306/r50551te_dbTraining_enroll
spring.datasource.username=r50551te_dbAdmin
spring.datasource.password=!Q2w#E4r5t^Y

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project

spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect


# Changing the port for heroku deploy
#spring.datasource.hikari.connection-timeout=30000
#spring.datasource.hikari.maximum-pool-size=10
#spring.profiles.active=production
#logging.level.org.springframework=INFO
#server.port=${PORT:5000}

spring.security.user.password=user
spring.security.user.name=user

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dbenrollment@gmail.com
spring.mail.password=dbenrollment1
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

